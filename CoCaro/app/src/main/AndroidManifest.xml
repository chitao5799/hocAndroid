<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cocaro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_caro"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".HumanVsMachine"></activity>
        <activity android:name=".LichSu" />
        <activity android:name=".TinhDiem2Nguoi" />
        <activity android:name=".TrangMenu" /> <!-- android:theme="@style/AppTheme" -->
        <activity android:name=".TrangChu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>